// Used only if this plugin is hosted on ci.jenkins.io. GitHub Actions (.github/workflows/ci.yml) is the default CI.
buildPlugin(useContainerAgent: true, configurations: [
    [platform: 'linux', jdk: 21],
    [platform: 'linux', jdk: 17],
])
